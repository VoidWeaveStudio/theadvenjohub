// src/features/game/ui/hudLayout.ts

// TopMenu sits at 24px and its 56px icons scale to 2x from their top edge, so it
// owns the screen down to ~136px. Everything centred at the top starts below that.
export const TOP_MENU_BAND_PX = 148;
