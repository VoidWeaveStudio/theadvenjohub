import { ShootingConfig } from './types';

export const SHOOTING_CONFIG: ShootingConfig = {
  damage: 25,
  fireRate: 120,
  maxAmmo: 30,
  reloadTime: 2000,
  muzzleOffsetY: 1.5,
  shootingAnimationDuration: 200,
};